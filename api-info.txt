← В конструкторе АПИ запрашивается конфиг из корня, от которого уже "пляшут" остальные запросы
→ congif.json


← В шапке в меню запрашивается user.get() по адресу "/user"
→ user.json
? Нужно для "авторизации" и показывания списка триггеров и настроек
! На главной (?) есть API-status на случай, если ответ не пришёл. Либо какая-то ошибка (при удалении тега, на который подписаны триггеры)


← Каждая страница (кроме страницы тегов) грузит теги методом tag.list() по адресу "/tag"
→ tag.json
? Не понятно зачем


** Страница триггеров **
1.  ← Просит теги методом tag.list() по адресу "/tag". Есть другие методы: delete, stats, data
    → tag.json

2.  ← Просит настройки пользователя settings.get() по адресу "/user/settings", чтобы вытащить пейджинг
    → settings.json

3.  ← Просит постраничный список trigger.page(page, size) по адресу "/tag?page&size"
    → triggers.json
    ? Не понятно, где и кем фильтруется. И зачем выбранные теги пишутся в куки
    ? Почему редактирование через alt+click, а просмотр по обычному клику

4.  ← Отправляет tag.data(tag, data) по адресу "tag/:tag/data"
    → ...
    ? Не понятно, что за инфу отправляет


** Страница тригера **
1.  ← Просит настройки пользователя settings.get() по адресу "/user/settings", чтобы вытащить пейджинг
    → settings.json

2.  ← Просит информацию о триггере trigger.get(id) по адресу "/trigger/:id"
    → trigger-data.json
    ! Частично используется для вывода информации, частично для попапа редактирования

3.  ← Просит информацию о состоянии trigger.state(id) по адресу "/trigger/:id/state"
    → trigger-state.json

4.  ← В информации о состоянии можно вызвать два метода trigger.remove_metric(id, metric) и trigger.maintenance(id, data) для удаления метрики и режима "не беспокоить" соответственно
    → ...

5.  ← Просит события event.page(page, size) по адресу "/event/:id?page&size"
    → trigger-event.json
    ! Строит график отношений каждой метрики. Строит список изменений состояния

6.  ← В информации о триггере можно вызвать метода trigger.reset_throttling(id)
    → ...
    ? Не понятно, что он делает и где в интерфейсе вызывается


** Страница настройки (a) / добавления (b) тригера **
1
    a.  ← Просит информацию о триггере trigger.get(id) по адресу "/trigger/:id"
        → trigger-data.json

    b.  ← Создаёт внутри себя "пустой аналог" trigger-data.json
        → ...
        ? Не понятно зачем

2.  ← Удаляет триггер trigger.delete(id) по адресу "/trigger/:id"
    → ...

3.  ← Сохраняет триггер trigger.save(trigger) по адресу "/trigger/:id"
    → Перенаправляет на страницу со списком триггеров
    ? Не понятно, как добавляется тег

4.  ← Сохраняет триггер из файла trigger.save(trigger) по адресу "/trigger/:id"
    → Перенаправляет на страницу со списком триггеров
    ? Не понятно, как происходит чтение и валидация


** Настройки пользователя **
1.  ← Просит настройки пользователя settings.get() по адресу "/user/settings"
    → settings.json

2.  ← Сохраняет способ связи contact.save(data) по адресу "/contact"
    → Данные по новому контакту
    ? Не понятно, как добавляются "субконтакты"

3.  ← Удаляет способ связи contact.delete(data) по адресу "/contact/:id"
    → ...

4.  ← subscription.save(subscription) "/subscription"
    → Данные по новой подписке

5.  ← subscription.delete(id) "/subscription/:id"
    → ...

6.  ← subscription.test(id) "/subscription/:id/test"
    → Данные по новой подписке и ...


** Скрытая страница тегов **
1.  ← Просит информацию о контактах contact.list() по адресу "/contact"
    → contact.json
    ! Для того, чтобы сопоставить с id тегах и вывести подписавшихся

2.  ← Просит информацию о тегах tag.stats() по адресу "/tag/stats"
    → stats.json

3.  ← Удаляет тег tag.delete(tag) по адресу "/tag/:tag"
    → ...

3.  ← Удаляет подписку на тег subscription.delete(id) по адресу "/subscription/:id"
    → ...


** Скрытая страница паттернов **
1.  ← Просит информацию о паттернах pattern.list() по адресу "/pattern"
    → pattern.json
    ! Очень медленно грузит

2.  ← Удаляет паттерн pattern.delete(pattern) по адресу "/pattern/:pattern"
    → В ответ "undefined"


** Скрытая страница уведомлений **
1.  ← Просит информацию об уведомлениях notification.list() по адресу "/notification?start=0&end=-1"
    → notification.json

2.  ← Удаляет подписку на уведомления notification.remove(id) по адресу "/notification?id"
    → ...


** Скрытая страница метрик **
1.  ← metric.get(trigger_id, start, end) по адресу "/metrics?params"
    → ...
    ? Есть в АПИ, на сайте не используется